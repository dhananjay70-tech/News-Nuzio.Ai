Implement **Phase 6: Node/Express Backend Integration** for the News Pulse / Nuzio AI assessment.

IMPORTANT: Phase 1–5 are COMPLETE and ALL TESTS PASS.

Do not rewrite or break any existing functionality.

Current completed architecture:

```text
RSS feeds
→ Python FastAPI AI Service
→ Article extraction
→ Cleaning
→ Deduplication
→ PostgreSQL
→ TF-IDF
→ Cosine similarity
→ Topic clustering
→ Cluster labels
→ FastAPI REST APIs
```

The Python AI service is located at:

```text
C:\Users\niran\Desktop\Nuzio.Ai\Ai_Service
```

FastAPI currently runs locally on:

```text
http://localhost:8000
```

Existing Python endpoints:

```text
GET  /clusters
GET  /clusters/{cluster_id}
GET  /timeline
POST /ingest/trigger
GET  /ingest/status/{job_id}
```

Phase 5 has already tested all of these successfully.

---

# OBJECTIVE

Connect the EXISTING Nuzio Node/Express backend to the Python FastAPI AI service.

The Node backend should act as the API gateway between the React frontend and Python AI service.

Architecture:

```text
React Frontend
      ↓
Nuzio Node/Express Backend
      ↓
Python FastAPI AI Service
      ↓
PostgreSQL
```

The React frontend should NOT directly call the Python service.

---

# 1. FIRST INSPECT THE EXISTING NODE BACKEND

Before modifying anything:

* Inspect the existing Nuzio Backend structure.
* Identify the Express entry point.
* Identify existing routes.
* Identify existing controllers/services.
* Identify existing environment configuration.
* Identify existing Axios/fetch HTTP client usage.
* Identify existing error-handling middleware.
* Identify existing CORS configuration.
* Identify the existing database setup.

Do NOT create duplicate Express servers.

Do NOT replace the existing backend.

Do NOT modify existing Nuzio news functionality unnecessarily.

---

# 2. PYTHON AI SERVICE URL

Add a configurable environment variable to the Node backend:

```env
AI_SERVICE_URL=http://localhost:8000
```

Do not hardcode:

```text
http://localhost:8000
```

inside route/controller code.

For production, this will later become the deployed FastAPI URL.

Do not expose secrets to the frontend.

---

# 3. CREATE NEWS PULSE SERVICE

Create a dedicated Node service/client, for example:

```text
services/newsPulseService.js
```

or follow the existing project's service naming convention if it already has one.

This service should communicate with FastAPI.

Implement methods:

```text
getClusters()
getClusterById(clusterId)
getTimeline(filters)
triggerIngestion()
getIngestionStatus(jobId)
```

Use the project's existing HTTP client if one already exists.

If there is no HTTP client, use Axios or the existing fetch implementation.

Do NOT add unnecessary dependencies.

---

# 4. NODE API ENDPOINTS

Create a dedicated route for News Pulse.

Use the existing Express architecture/style.

Required endpoints:

```text
GET /api/news-pulse/clusters
GET /api/news-pulse/clusters/:id
GET /api/news-pulse/timeline
POST /api/news-pulse/ingest/trigger
GET /api/news-pulse/ingest/status/:jobId
```

---

# 5. GET /api/news-pulse/clusters

Node should call:

```text
GET ${AI_SERVICE_URL}/clusters
```

and return the FastAPI response.

Example:

```json
{
  "clusters": [
    {
      "id": 1,
      "label": "OpenAI AI Model",
      "article_count": 4,
      "latest_published_at": "2026-09-24T10:30:00Z"
    }
  ],
  "count": 1
}
```

Do not unnecessarily transform the response unless the existing backend architecture requires it.

---

# 6. GET /api/news-pulse/clusters/:id

Node should call:

```text
GET ${AI_SERVICE_URL}/clusters/:id
```

Forward the response.

If FastAPI returns 404, Node should return 404.

Do not convert every Python error into HTTP 500.

Preserve meaningful status codes.

---

# 7. GET /api/news-pulse/timeline

Node should support forwarding these query parameters:

```text
source
cluster_id
from
to
limit
```

Example:

```text
GET /api/news-pulse/timeline?source=BBC%20News&limit=20
```

Node forwards:

```text
GET ${AI_SERVICE_URL}/timeline?source=BBC%20News&limit=20
```

Do not manually rebuild query strings unsafely.

Use a proper URL/query parameter mechanism.

---

# 8. POST /api/news-pulse/ingest/trigger

Node calls:

```text
POST ${AI_SERVICE_URL}/ingest/trigger
```

Return the job ID to the frontend.

Example:

```json
{
  "job_id": "e92a85126b58",
  "status": "QUEUED"
}
```

Do not wait for ingestion to finish.

The Python service already runs ingestion in the background.

---

# 9. GET /api/news-pulse/ingest/status/:jobId

Node calls:

```text
GET ${AI_SERVICE_URL}/ingest/status/:jobId
```

Return:

```json
{
  "job_id": "e92a85126b58",
  "status": "COMPLETED",
  "articles_fetched": 20,
  "articles_inserted": 12,
  "articles_skipped": 8,
  "clusters_created": 4,
  "error": null
}
```

The frontend will later poll this endpoint.

---

# 10. ERROR HANDLING

Implement clean handling for:

### Python service unavailable

Return something like:

```json
{
  "error": "News intelligence service unavailable"
}
```

with HTTP 503 where appropriate.

### Python 404

Forward 404.

### Python 400

Forward 400.

### Python 500

Return a clean backend error without exposing stack traces.

Never expose:

* database passwords
* DATABASE_URL
* internal stack traces
* Python filesystem paths

Log detailed errors server-side.

---

# 11. TIMEOUTS

Set a reasonable HTTP timeout when Node communicates with FastAPI.

Do NOT make the Node request wait indefinitely.

Important:

```text
POST ingest/trigger
```

should return quickly because Python already performs ingestion in the background.

The frontend will poll the status endpoint.

---

# 12. CORS

Do NOT create a second CORS system unnecessarily.

Use the existing Node backend CORS configuration.

The React frontend should call:

```text
Node backend
```

not:

```text
Python FastAPI
```

---

# 13. TESTING

Create tests following the existing Nuzio backend testing style.

Test:

```text
GET  /api/news-pulse/clusters
GET  /api/news-pulse/clusters/:id
GET  /api/news-pulse/timeline
POST /api/news-pulse/ingest/trigger
GET  /api/news-pulse/ingest/status/:jobId
```

Mock the Python service for unit/integration tests where appropriate.

Do not make every Node test depend on live RSS scraping.

Test:

* successful response
* 404
* 400
* Python service unavailable
* timeout
* invalid cluster ID
* query parameter forwarding

---

# 14. MANUAL END-TO-END TEST

Start Python service:

```powershell
cd C:\Users\niran\Desktop\Nuzio.Ai\Ai_Service
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Then start the existing Nuzio Node backend using its normal development command.

Verify:

```text
GET http://localhost:<NODE_PORT>/api/news-pulse/clusters
```

Then:

```text
GET http://localhost:<NODE_PORT>/api/news-pulse/timeline
```

Then:

```text
POST http://localhost:<NODE_PORT>/api/news-pulse/ingest/trigger
```

Take the returned `job_id`.

Poll:

```text
GET http://localhost:<NODE_PORT>/api/news-pulse/ingest/status/<job_id>
```

Verify the status eventually becomes:

```text
COMPLETED
```

---

# 15. DO NOT MODIFY THESE

Do NOT modify:

```text
nuzio_ai."NewsArticle"
nuzio_ai."User"
nuzio_ai."NewsSource"
```

Do not remove existing Nuzio features.

Do not move the AI clustering code into Node.

Do not merge Python and Node into one application.

Do not introduce:

* Docker
* Redis
* Celery
* Kafka
* Prisma
* TypeScript

---

# 16. README

Update documentation with:

```text
React
 ↓
Node API
 ↓
Python AI Service
 ↓
PostgreSQL
```

Document:

```env
AI_SERVICE_URL=http://localhost:8000
```

Document all Node News Pulse endpoints.

Document how to run both services locally.

---

# 17. REGRESSION

After implementation, run the existing Node backend tests.

Also verify the Python Phase 1–5 tests still pass:

```powershell
python test_phase5.py
python test_phase4.py
python test_phase3.py
python test_phase2.py
python test_phase1.py
```

Do not modify Python Phase 1–5 unless a genuine integration issue requires it.

---

# STOP CONDITION

Do NOT start frontend work.

Do NOT start Phase 7.

Stop after Phase 6.

Provide a final report:

1. Existing Node architecture inspected
2. Files created
3. Files modified
4. `AI_SERVICE_URL` configuration
5. Node endpoints created
6. FastAPI endpoints connected
7. Error handling
8. Tests executed
9. Test results
10. Manual end-to-end results
11. Any issues

Only proceed to Phase 7 after Phase 6 is completely tested.
